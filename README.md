# Personal portfolio

One-page portfolio of Muhammad Azniezul Islami bin Abdul Razak: a single `index.html`, no build step.

- `index.html` — the page
- `DESIGN.md` — the visual system the page follows (colours, type ramp, spacing rules)
- `PRODUCT.md` — who the page is for and what it must stay true to

Published with GitHub Pages from the `main` branch.
